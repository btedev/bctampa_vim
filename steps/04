Intermediate Editing
====================

(use resque_test.rb)

1.  dw = delete word

2.  db = delete word backward

3.  diw = delete inside word (whole)

4.  2dw = delete next two words

5.  d$ = delete to end of line

6.  d^ = delete to beginning of line

7.  df( = delete to & including (

8.  dt( = delete up to (

9.  dG = delete to end of file

10.  di) = delete inside parens
    (also di] di' di")

11.  dd = delete line (also D)

12. J = join this line and next

13. ciw = same as diw, but then start insert mode (also ci" etc.)

14. ddp = switch this line and the next

15. xp = transpose two characters 
